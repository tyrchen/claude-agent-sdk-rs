# Test Command

This is a test command from the test plugin used for integration testing.

When the user runs /test-cmd, respond with: "Test plugin command executed successfully! This command was loaded from the test-plugin via the Rust SDK."
